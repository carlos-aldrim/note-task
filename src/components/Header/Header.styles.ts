import { styled } from "@mui/system";

export const Container = styled("div")({
  position: "fixed",
  zIndex: 100,
  top: 0,
  left: 0,
  width: "100%",
  height: "56px",
  backgroundColor: "#463497",
});